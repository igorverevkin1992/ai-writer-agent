# АУДИТ ПРОЕКТА — находки и план устранения

Дата: 03.09.2026. Пять независимых проходов (безопасность и защита канона; корректность конвейера
и FSM; разбор реальной библиотеки, окно и проверки; React-панель и HTML; тесты, CI, упаковка,
документация). Каждая находка ниже проверена на коде и, где возможно, воспроизведена на реальной
библиотеке `УГАР_Библиотека/`. Тесты в момент аудита: 125 зелёных, покрытие 87 %.

Шкала: **К** — критично (нарушает гарантию ТЗ или ломает данные), **В** — важно, **М** — мелочь.

---

## 1. Защита канона и FR-C3 (что видит Писатель)

| № | Ур. | Находка | Где |
|---|---|---|---|
| 1.1 | К | **Досье в окне сводят на нет FR-C3.** Профиль и отношения вставляются целиком: в окне гл. 5 (фокал Степан) досье Лемма содержит «спящий актив „Континенталя“», «Штерн — сын», досье Степана — «завербован ОГПУ», «с т.6 — знание», «с т.9 — брат». В окне гл. 1 — «гибнет в чистках т.6», гл. 46 — Нюрнберг т.11. Тест проверяет только буквальную строку «сын Лемма», поэтому проходит. | `compiler.py:128`, `realcanon.parse_dossiers_real`, `окно.md.j2:36–45` |
| 1.2 | К | **Частичное/неверное знание печатается как полное.** Курсив матрицы даёт `note="частично: …"`, но окно и Э2 выводят `fact`: гл. 41 «[М-16] В кабаре присутствовал Мередит» при канонном «фигура холода, без опознания»; гл. 46 Штерн «остаток в золе»; гл. 30/35 Степан М-08/М-12. | `realcanon.py:287–303`, `compiler.py:119`, `verifier2.py:54–58` |
| 1.3 | К | **Тайна Т-06 («Подлог 1913») разобрана как «не раскрывается в томе»** (ячейка «Часть IV, третья доза» без «гл.»), а бриф гл. 32 требует «полная картина подлога». В окнах 32–46 висит противоположный приказ. Та же ошибка — Т-10. | `realcanon.py:383–389` |
| 1.4 | К | **Э2 получает все тайны как действующие запреты** независимо от главы: фильтр учитывает только `until_volume`. В гл. 46 модель обязана флагнуть раскрытие Т-05, которое в этой главе и происходит. | `verifier2.py:81` vs `compiler._ban_active` |
| 1.5 | В | «Красный Крест / Ватикан» (резерв т.6, «ноль в томе 1») разобран как закладка без глав и **ни в одно окно не попадает как запрет**. | `realcanon.parse_plants_registry` |
| 1.6 | В | Т-04: «Честные улики с гл. 4» принято за главу раскрытия → с гл. 5 запрет «резидент — судмедэксперт» исчезает, хотя разгадка «≈ гл. 20». | `realcanon.py:383` |
| 1.7 | В | Guard хранит путь библиотеки в thread-local: в потоках HTTP-сервера, не вызывавших `_ctx()`, **запись в библиотеку не блокируется**. Сейчас эксплуатации нет (пути фиксированы), но гарантия FR-K3 в панели формально не действует. | `guard.py:14–26`, `server.py:233, 259, 278, 295, 484` |

## 2. Конвейер, git и FSM

| № | Ур. | Находка | Где |
|---|---|---|---|
| 2.1 | К | **Git-операции не ограничены папкой библиотеки**, а библиотека лежит внутри репозитория кода. `git add -A` без pathspec: коммит приёмки `[глава N]` захватывает весь рабочий каталог (panel/src, README, круги_истории…), `dirty()` даёт ложный отказ при любом изменении вне библиотеки, `rollback` ревертит посторонние файлы. «Атомарный коммит приёмки» (FR-K2) в поставляемой раскладке не выполняется. | `gitops.py:32, 57, 78`, `canonist.py:177` |
| 2.2 | К | **Поиск коммита приёмки ловит и `Revert "[глава N]…"`** → повторный откат ревертит реверт: глава возвращается в канон, FSM говорит «принято». Усугубляется тем, что revert делается до обновления `status.yaml`, а ошибка экспорта после него оставляет «зафиксировано». | `gitops.py:47–54`, `cli.py:826–843` |
| 2.3 | К | **Повторный цикл правок стартует от испорченного черновика.** После красного дифф-контроля `apply-edits` берёт `st.draft` (draft_2 с самоволиями), дифф сравнивает 2↔3 — самоволия «отмываются», accept проходит. ТЗ FR-E3: цикл повторяется от текста, принятого на приёмке. | `cli.py:267–288`, `verifier1.py:344` |
| 2.4 | В | Парсер `edits.md`: пустое «СТАЛО» (удаление) невозможно и проглатывает следующую пару; одиночное «БЫЛО» портит соседнюю правку; «УКАЗАНИЕ» вплотную попадает в «СТАЛО» и дублируется. | `review.py:103–106` |
| 2.5 | В | Дифф-контроль: ложное «не внесено», если цитата «БЫЛО» встречается в тексте дважды; ложное «чисто», если блок изменений объяснён хотя бы одной правкой; `--авторская-правка` обнуляет все самоволия разом. | `verifier1.py:352, 369–373` |
| 2.6 | В | `apply_batch` не атомарен: ошибка после записи в библиотеку (MarkupError экспорта, отказ git, невалидный `commit_author` «Имя» без email) оставляет библиотеку грязной, повтор заблокирован, FSM не переведён. Без git FSM всё равно уходит в терминальное «зафиксировано», откуда откат невозможен. | `canonist.py:216–260`, `config.py:43` |
| 2.7 | В | `REGISTRY_GLOBS` не соответствуют реальной библиотеке: файла `32_*` нет (закладки в §7 реестра), в 33/12 нет таблиц → `_append_table_row` дописывает сиротскую строку, которую экспортёр молча игнорирует (запись теряется); строка для 3.1 раскладывается по колонкам неверно; статус закладок (FR-K1) для реального канона не обновляется. | `canonist.py:21–26, 137–161, 263–286` |
| 2.8 | В | Регрессия «зелёная» при 0 тестов, при всех пропущенных Э2 и при устаревшем отчёте → `retest --зафиксировать` и `doctor` проходят вакуумно (FR-R3). В корне репозитория корпуса нет вовсе. | `regression.py:113–132`, `cli.py:961` |
| 2.9 | В | Фантомные самоволки: `resolutions.json` не пересобирается, если новых самоволок нет → после повторного verify2 accept блокируется старым F-001. | `review.py:93–98` |
| 2.10 | В | `retest` пересобирает `window.md` главы в работе без FSM-предупреждения. | `cli.py:972–976` |
| 2.11 | М | «Нечего коммитить» возвращает HEAD → глава «зафиксирована» без коммита `[глава N]`; повторный `canonize N` перезаписывает отредактированный пакет и тратит вызов LLM; `verdict.json`/`flags.json` перезаписываются при повторах (NFR-4); авто-повтор Э1 не попадает в историю; `status.yaml` пишется неатомарно; `rollback` без `--to` после отката ищет «предыдущее» по истории, а не по цепочке состояний; `test_id` не санитизируется как имя файла; `_friendly` глотает программные ошибки без трейсбека. | `gitops.py:33`, `cli.py:380, 818`, `verifier1.py:136`, `fsm.py:64` |

## 3. Разбор канона и проверки Э1

| № | Ур. | Находка | Где |
|---|---|---|---|
| 3.1 | В | Закладки с перечислением глав («Гл. 9, 27», «Гл. 34, 40», «Гл. 29 или 40») получают только первую главу → окна гл. 27 и 40 «закладок нет». | `realcanon.py:325` |
| 3.2 | В | Деление на предложения: инициалы «А. К. Штерн.» дают три предложения; мягкие переносы строк внутри абзаца считаются границами (гл. 5: средняя 5,78 вместо 6,16); однобуквенные сокращения «им.», «с.» склеивают предложения. Метрика Э1, на которой стоит калибровка, искажена. | `textutils.py:64–86`, `сокращения.txt` |
| 3.3 | В | Стоп-лексика ищется только точной словоформой: «отца», «сыну» не ловятся стоп-листом Штерна. FR-V1.5 для русского почти не работает. | `verifier1.py:73–80` |
| 3.4 | В | `ugar check --глава N` сравнивает текст с самим собой в корпусе → ложный флаг межглавного повтора. | `cli.py:582–591` |
| 3.5 | В | Участники сцены не извлекаются из событий реестра (гл. 10–46), «куратор ОГПУ» не матчится по регистру; гл. 22 «Заварзин глазами Лемма» теряет Заварзина в досье и срезе Э2. | `realcanon.parse_registry_briefs`, `:257–260` |
| 3.6 | В | Инструкции «→ ДОКУМЕНТ №N» поглавника не попадают в бриф: Писатель не знает, что писать вставку. Поле сцены «входит:/выходит:» (Мауэри) отбрасывается. | `realcanon.enrich_from_poglavnik` |
| 3.7 | М | Жирный `**гл.46**` в матрице принят за курсив; континуити без дат и с ложными «т.8»; `томов 2–5` → [2]; «глухарь ⚠»/«мент ⚠» не экспортируются как флаг-слова; отношения Аси без тире не разбираются; TTR-окно «по части» строится по всему корпусу; формы «был/была/были/было» не зафиксированы каноном; «Глава пятая» считается предложением. | `realcanon.py`, `verifier1.py:313`, `textutils.py` |

## 4. Сервер панели и веб-безопасность

| № | Ур. | Находка | Где |
|---|---|---|---|
| 4.1 | К | **XSS через `flag_id` из ответа модели.** Идентификатор не валидируется и подставляется в `id="…"`/`href` без экранирования в панели (через `dangerouslySetInnerHTML`) и в `review.html`. Prompt-injection в тексте главы → скрипт в origin панели → POST `canonize-apply` с нужным заголовком: обход FR-K3 силами недоверенного вывода модели. | `schemas.py:178`, `ChapterView.tsx:171`, `htmlreview.py:69, 125` |
| 4.2 | В | Нет проверки `Host`/`Origin` → защита `X-Ugar-Panel` обходится DNS-rebinding: чужой сайт становится same-origin для 127.0.0.1:8765 и может читать рукопись/канон и запускать команды. | `server.py:447–507` |
| 4.3 | В | GET с побочными эффектами (`/prompt/verify2` пишет файл, `/dashboard` пишет HTML) — дёргаются с любого сайта без заголовка. | `server.py:226–234, 481–485` |
| 4.4 | В | Гонка `redirect_stdout`: `ensure_idle` без блокировки; синхронные `accept/rollback/manual_*` не помечают «занято» → параллельная задача навсегда подменяет `sys.stdout` процесса. | `server.py:68–83, 287–341` |
| 4.5 | В | Windows: `subprocess.run(text=True)` без `encoding="utf-8"` читает вывод git в cp1251 → `find_chapter_commit` не матчит «[глава N]», откат зафиксированной главы на русской Windows невозможен. | `gitops.py:11–13` |
| 4.6 | М | Тело POST без лимита; `manual_draft` пишет файл до проверки FSM; `manual_flags` затирает `flags.json` до проверки; `scope` круга не валидируется (битый файл → 500 на `/api/circles` навсегда); `target_registry` не валидируется; неизвестный `kind` → 500 вместо 400; ретраи SDK умножаются на ретраи приложения (до 9 попыток × 120 с); ошибки Gemini 400/403 ретраятся как временные. | `server.py`, `adapters.py:39–47, 146` |

## 5. React-панель и HTML

| № | Ур. | Находка | Где |
|---|---|---|---|
| 5.1 | К | **Опрос `/api/state` вырождается в бесконечный цикл без задержки**, как только появилась любая задача: `prevJob` меняет identity на каждом ответе → `refresh` пересоздаётся → `useEffect` перезапускается немедленно. Сервер на каждый запрос читает статусы всех 46 глав и отдаёт весь лог задачи. | `App.tsx:176–194` |
| 5.2 | В | `refreshTick` не срабатывает для быстрых задач (compile, отказ FSM, ручной режим) → карточка главы и кнопки устаревают, повторный клик даёт «переход недопустим». | `App.tsx:181–210` |
| 5.3 | В | Пересекающиеся цитаты Э1/Э2 ломают разметку подсветки: замена попадает внутрь `title` предыдущего `<mark>` — текст «чтения с флагами» теряется/дублируется. | `ChapterView.tsx:166–181`, `htmlreview.py:193–231` |
| 5.4 | В | Двойная отправка: кнопки без pending-состояния → двойной клик регистрирует ручной черновик дважды (draft_k+1 дубликат), то же у accept/rollback/resolve. | `ChapterView.tsx:464–512` |
| 5.5 | В | `/api/state` тащит весь `job.output` каждую секунду. | `server.py:48, 155` |
| 5.6 | В | Кнопки решений по самоволкам показываются в «верифицировано-2», когда `resolutions.json` ещё нет → 400. Нет пути к `diff-check --авторская-правка` из панели: после ручной правки «Принять главу» заблокирована навсегда, только CLI. | `ChapterView.tsx:245–259, 7–28` |
| 5.7 | М | `window.confirm` как единственный предохранитель необратимых действий (браузер может подавить диалог → тихий отказ); `String.replace` со строкой-заменой (`$&`); 46 захардкожено; таймер тоста; очередь глав недоступна с клавиатуры; статика → 403, если `ugar/data` за симлинком. | `ChapterView.tsx`, `Circles.tsx:47`, `App.tsx:171`, `server.py:493` |

## 6. Тесты, CI, упаковка, документация

| № | Ур. | Находка | Где |
|---|---|---|---|
| 6.1 | В | Откат зафиксированной главы через `git revert` — 0 % покрытия (единственный путь изменения канона в обход `canon_write_session`). | `cli.py:828–843`, `gitops.revert` |
| 6.2 | В | CI только Ubuntu, целевая платформа автора — Windows (кириллические пути, кодировки, `.bat`). | `.github/workflows/tests.yml` |
| 6.3 | В | Не покрыты: `retest`/`add-golden` через CLI, ветки `run` после приёмки, эндпоинты `/rollback`, `/draft`, `/diff`, `/log`, `/canon-batch`, ручной apply-edits из панели, `draft_k.meta.json` (Д-6). `.[llm]` в CI не ставится — SDK-ветки адаптеров не импортируются даже на синтаксис. | `tests/`, CI |
| 6.4 | М | Тавтологические утверждения (`… or True`, `A and B or C`); `.gitignore` без `snapshots/`, `круги_истории/`, `.coverage`; ЗАПУСК.md привязан к ветке и личному пути; `npm install` vs `npm ci`; зависимости без верхних границ; нет статического теста «в коде нет порогов» и «нет обхода guard»; FR-C4 проверяется в одном процессе. | `tests/test_product.py:43`, `tests/test_panel.py:70`, `.gitignore`, `pyproject.toml` |

---

## План устранения

Порядок — по риску для канона и рукописи. Каждый этап заканчивается тестом, который ловит именно
эту ошибку, и полным прогоном. Правила канона (что именно входит в окно из досье, формы «был»,
статус закладок) — решения автора: там, где нужна запись Р-№, это указано.

### Этап 1. Гарантии ТЗ: FR-C3, FR-K2/K3, FR-E3 (1.1–1.4, 1.7, 2.1–2.3, 4.1)

1. **Досье в окне (1.1).** В окно — только «Профиль» (первые 2–3 предложения), «Физика», «Речевой
   паспорт»; отношения — только к участникам сцены; фрагменты с «т.N» будущих томов и «Ф-19xx»
   вырезаются; «Психологический каркас», «Арка», «Статус» не выводятся (они нужны Писателю в
   другой форме — см. этап 5). Решение Р-№: правило «что из досье видит Писатель» в соглашениях
   разметки. Тест: скан всех 46 окон на список запретных маркеров тайн из реестра и на «т.[2-9]».
2. **Частичное знание (1.2).** Для фактов с пометкой «частично» окно и срез Э2 печатают текст
   пометки, не сам факт. Тест на М-16/гл. 41 и М-03/гл. 46.
3. **Тайны без «гл. N» (1.3, 1.6).** Главу раскрытия брать из матрицы (колонка «Читатель») либо из
   границ части; «улики с гл. 4» не считать раскрытием. Тест: Т-06 → 32, Т-04 → ≈20, Т-10.
4. **Единый фильтр запретов (1.4).** `_ban_active` вынести в общий модуль, использовать в
   компиляторе и Э2. Тест: промпт Э2 гл. 46 не содержит Т-05 как запрет.
5. **Guard без thread-local (1.7).** Путь библиотеки — модульная переменная, задаётся в `serve()`;
   thread-local только для сессии записи. Тест: запись в библиотеку из дочернего потока падает.
6. **Git только в пределах библиотеки (2.1).** `add -A -- .`, `status --porcelain -- .`, после
   `revert` — проверка, что затронуты только пути библиотеки; `doctor` предупреждает, если
   библиотека не является корнем своего репозитория. Тест на репозитории с посторонним файлом.
7. **Откат по SHA (2.2).** При `canonize --apply` записывать SHA коммита в `status.yaml`;
   `rollback` откатывает по нему, пропускает `Revert "`; статус обновлять сразу после реверта.
   Тест: двойной rollback не возвращает главу в канон.
8. **База цикла правок (2.3).** В `review` фиксировать `база_приёмки`; `apply-edits` и
   `diff-check` работают от неё. Тест: самоволие draft_2 остаётся видимым в дифф-контроле draft_3.
9. **Валидация `flag_id` и атрибутов (4.1).** `Flag.flag_id` и `Resolution.flag_id` —
   `pattern=^[\w.\-]+$`, `target_registry` — по списку реестров; экранирование атрибутов в
   `htmlreview` и панели. Тест: флаг с кавычкой и `<img onerror>` отвергается.

### Этап 2. Целостность данных конвейера (2.4–2.9, 2.11, 4.4–4.6)

10. Построчный парсер `edits.md` с пустым «СТАЛО» и ошибками по номеру строки (2.4).
11. Дифф-контроль по счётчикам вхождений и по каждому изменённому предложению; `--авторская-правка`
    с перечнем фрагментов (2.5).
12. Откат библиотеки при сбое `apply_batch` (`git checkout -- .` + `clean` в пределах библиотеки),
    валидация `commit_author` в конфиге, запрет `--apply` без git (2.6).
13. Реестры реальной библиотеки: если у файла нет таблицы с нужными заголовками — во «Входящие»;
    строка 3.1 по заголовку; статус закладок в §7 реестра (2.7). Решение автора: где должны жить
    новые факты 3.3/3.5 (таблица или буллеты).
14. Регрессия: «зелёная» только при непустом корпусе и без пропусков; в отчёт — хэши конфига,
    шаблонов и норм; устаревший отчёт = «не запускалась» (2.8). Плюс: пополнить корпус
    настоящими «красными» текстами отборочного теста (задача автора, FR-R4).
15. Пересборка `resolutions.json` при каждом `review` с сохранением решений (2.9); `retest` собирает
    окно во временную папку (2.10); `verdict_k.json`/`flags_k.json` вместо перезаписи, запись
    авто-повтора в историю, атомарная запись YAML/JSON через временный файл, `--allow-empty`
    или ошибка при пустом коммите, `--заново` для повторного `canonize` (2.11).
16. Одна блокировка на все операции с захватом вывода в сервере (4.4); `encoding="utf-8"` во
    всех `subprocess.run` и `PYTHONUTF8=1` в лаунчере (4.5); проверка состояния до записи,
    валидация `scope`/`registry`/`kind`, лимит тела запроса, `max_retries=0` у SDK-клиентов и
    учёт `code` у ошибок Gemini (4.6).

### Этап 3. Точность разбора и Э1 (1.5, 3.1–3.7)

17. Все главы из перечислений закладок; «НЕ упоминается в томе N» → запрет информрежима (3.1, 1.5).
18. Сплиттер: абзац = блок до пустой строки, инициалы, однобуквенные сокращения по контексту (3.2).
    После правки — перекалибровка теста 10.1 на макете гл. 4 и фиксация форм «был» в 02 §5 (Р-№).
19. Стоп-лексика по основам слов (3.3); `own_stem` в `check` (3.4); участники из событий реестра и
    «глазами X» (3.5); «→ ДОКУМЕНТ» и «входит/выходит» в бриф (3.6); мелочи 3.7.

### Этап 4. Панель (5.1–5.7, 4.2–4.3)

20. `prevJob` в `useRef`, событие завершения по `started`/статусу, ответ POST задаёт `job` (5.1, 5.2).
21. `/api/state` без полного `output` (хвост + длина), полный лог по `/api/job` (5.5).
22. Подсветка за один проход по позициям с отбрасыванием пересечений — общая функция для панели и
    `review.html` (5.3). Pending-состояние на каждой кнопке (5.4). Кнопки решений только при наличии
    `resolutions.json`; действие «Дифф-контроль как авторская правка» с подтверждением (5.6).
23. Проверка `Host`/`Origin` на сервере, побочные эффекты только в POST (4.2, 4.3).
24. Собственный модальный диалог подтверждения вместо `window.confirm`; клавиатурная навигация по
    очереди; фильтр глав (5.7).

### Этап 5. Тесты, CI, документация (6.1–6.4)

25. Тесты: откат через `git revert` (двойной откат, грязная библиотека), `retest`/`add-golden`,
    `run` до «принято», все эндпоинты панели, ручной цикл из панели, `meta.json` черновика,
    межпроцессный детерминизм, AST-тесты «нет порогов в коде» и «нет записи мимо guard»,
    сканер окон на утечки (п. 1). Убрать тавтологические утверждения.
26. CI: матрица с `windows-latest`, job с `.[llm,dev]` и фейковыми SDK-клиентами.
27. `.gitignore` (`snapshots/`, `круги_истории/`, `.coverage`), ЗАПУСК.md без привязки к ветке и
    личному пути, единая команда сборки панели, минимальные версии зависимостей по факту.
28. Методика (из ответа про техники): каркас Уэйланда из досье в окно отдельной секцией «Где герой
    на арке» — после п. 1, чтобы не вернуть утечку; сцена по Мауэри как структура; чек-лист 4.4
    дополнен — всё по решениям Р-№.

### Оценка объёма

| Этап | Находок | Ориентир |
|---|---|---|
| 1. Гарантии ТЗ | 9 | 1–2 дня |
| 2. Целостность данных | 7 | 2 дня |
| 3. Разбор и Э1 | 7 | 1–2 дня |
| 4. Панель | 9 | 1 день |
| 5. Тесты, CI, документация | 4 группы | 1 день |

До этапа 1 конвейер нельзя использовать для генерации глав: окно Писателя раскрывает тайны серии
(1.1–1.3), а приёмка может испортить репозиторий кода (2.1) и отмыть самоволия (2.3).
