# АУДИТ ПРОЕКТА — второй проход: находки и план усовершенствования

Дата: 08.09.2026. Ветка `claude/konveyer-ugar-spec-8g3apy`. Первый аудит (`АУДИТ.md`, 03.09) выполнен
полностью; здесь — только новые и остаточные находки. Четыре независимых прохода: бэкенд
(корректность, гонки, масштаб, безопасность); разбор реального канона, окно Писателя, Э1/Э2 и линтер;
React-панель и UX (живой прогон в Chromium на демо- и реальной библиотеке, 33 скриншота); продукт и
процесс (драматургия, цикл автора, экономика, риски). Каждая находка проверена на коде; где указано
«воспр.» — воспроизведена скриптом или мутацией библиотеки (26 мутированных копий канона для линтера,
46 собранных окон, промпт Э2 по принятой гл. 5).

Состояние на момент аудита: 228 тестов зелёные, покрытие 91 %; `ruff` (E,F,W,B,SIM): 98 замечаний,
из них 13 неиспользуемых импортов и 11 `zip()` без `strict`. Реальная библиотека: линтер — 0 ошибок,
1 предупреждение (Т-07: реестр «гл. 17» vs матрица «гл. 18»). `doctor`: один remote (нужно ≥ 2),
регрессионный корпус пуст.

Шкала: **К** — ломает гарантию ТЗ, теряет данные или делает шаг невозможным; **В** — искажает канон,
проверку или работу автора; **М** — мелочь.

**Уже исправлено в этом проходе** (коммит «Панель „Канон“: версия документа по хэшу содержимого»):
кнопка «Сохранить» вида «Канон» не работала из браузера — сервер сравнивал `st_mtime_ns`
(≈1,79·10¹⁸), а JavaScript хранит числа как double (2⁵³): после JSON число округлялось, и каждое
сохранение получало «документ изменён на диске». Версия теперь — sha256 содержимого, проверка внутри
замка задач; «Применить исправление» передаёт ровно то `file/line/old/new`, что автор подтвердил, а не
индекс в отчёте, который наблюдатель мог перестроить; переход к находке в открытом документе не требует
«потерять правки». Проверено в Chromium: два сохранения подряд проходят.

---

## 0. Главные выводы

1. **Писатель пишет каждую главу вслепую.** В окне нет ничего о предыдущих главах, срез матрицы 3.1 —
   это тайны, а не знание фокала; секции «Запреты» и «НЕ знает» для реального канона всегда пусты;
   континуити 3.3 в окно не входит; калибровка Р-015 (§5 документа 02, эталоны §6.3) в окно не попадает,
   а §2 («короткая фраза — норма») толкает модель в тот телеграф, который Р-015 объявил браком.
   Собранное окно беднее ручного эталонного промпта гл. 5 в секциях, определяющих прозу.
2. **Целый слой канона невидим машине.** Разделы 5 («Три дозы 1913») и 6 («Реестр документов») реестра
   и колонка «Что нового знает читатель» не экспортируются: окна гл. 12, 14, 21, 22, 32, 35, 46 не знают
   ни доз, ни документов-вставок, ни «печи в мае». Хронология 12 и хроника 17 не экспортируются вовсе.
3. **FR-C3 держится, но течёт через отношения.** Строка досье «Степан — вектор: расчёт, проросший
   настоящим» (содержание Т-07 и арка т. 4) печатается в окнах фокала Степана гл. 2–44; «Ася — от искры
   (т.1) к браку» обходит фильтр будущего, потому что первая ссылка не будущая.
4. **Линтер ловит форму, но не смысл.** Из 26 мутаций канона (дозы, документы, даты vs хроника, знание
   раньше события, раскрытие тайны в главе не знающего фокала, закладка у чужого фокала, ухо
   левое/правое, возраст в абсолютном году, поглавник vs реестр, акты vs части) сработали три. Ложные
   ЭПИСТ-1/ПРОЗА-1 от подстрок («сынок» → «сын», «активно» → «актив») и от реплик чужих персонажей.
5. **Принятая в канон гл. 5 не проходит Э1 по текущим нормам** (средняя 6,2 при норме 9–12; 63 %
   коротких; 6 усилителей; 8 однострочных абзацев): эталон канона противоречит канону (Р-015 vs Р-018).
   TTR-окно никогда не считается (часть ≤ 8 000 слов < 10 000).
6. **Цикл приёмки не идемпотентен и не защищён от опечаток:** повторный `canonize --apply` после сбоя
   между коммитом и сменой состояния дублирует записи канона; `rollback --to <опечатка>` сначала ревертит
   канон, потом валидирует; `revert` с конфликтом оставляет маркеры `<<<<<<<` в документе канона.
7. **Панель теряет текст автора молча:** уход из вида «Канон» или переключение вкладок главы
   (edits.md, пакет канониста, вставленный черновик на 30 КБ) уничтожает несохранённое без вопроса.
8. **Для 11 томов конвейер технически не готов:** `chapters/005` не знает тома, документы 2.1/2.2/3.1
   берутся первым glob-совпадением, линтер и корпус смешают тома; рабочая область (черновики, правки,
   журнал) не бэкапится ничем; библиотека живёт в репозитории кода, и `git pull` кода принесёт автору
   конфликты с его каноническими коммитами.

---

## 1. Разбор канона и окно Писателя

| № | Ур. | Находка | Где | Воспр. |
|---|---|---|---|---|
| 1.1 | К | Разделы 5 («Три дозы 1913») и 6 («Реестр документов») реестра не экспортируются; колонка «Что нового знает читатель» отбрасывается. `briefs.json`: гл. 12 без дозы и документа, гл. 14/21/35 без документов, гл. 32 без «полной картины подлога». Единственный разрешённый канал прошлого и весь документный слой тома невидимы. | `realcanon.py:267` `parse_registry_briefs`, `exporter.py:295` | да |
| 1.2 | К | Следствие 1.1 в окнах: гл. 12 — закладка «[З-05] «Печь в мае»» без триггера, содержания, запрета и правила доз («интонация протокольная, сентимент запрещён»); гл. 46 (reveal серии) — бит «память впервые касается запретного: печь в мае» без объяснения, что это (Ф-1913-02). Гл. 14/21/35 — окно не требует документа-вставки; гл. 44 — без стиля и «нестыковки маршрута». | `окно.md.j2`, `compiler.py` | да (окна 012/022/032/046) |
| 1.3 | К | Отношения из досье раскрывают тайну в голове не знающего фокала: «Степан — вектор: расчёт, проросший настоящим» (Т-07 + арка т. 4) в окнах Степана гл. 2, 5, 8, 30, 38, 44; «Ася — от искры (т.1) к браку», «Степан — от профессионального уважения к любви» (гл. 14/23) — `_FUTURE_RE` пропускает, потому что первая ссылка «т.1». Маркеры Т-07 слово «расчёт» не покрывают. Туда же: обрывки списков после фильтра («Тройная идентичность: … для ОГПУ (Заварзин) — карманный инструмент;» в окнах Степана гл. 2–8 — владение Заварзина читатель узнаёт в гл. 10) и инструкции инструменту («держать в каждой сцене; инструмент обязан проверять корреляцию»). | `compiler.py:100–127` `safe_dossier`, `_safe_sentences` | да (скан 46 окон) |
| 1.4 | В | Тайна Т-10 («Кто убил поляка») сопоставлена с М-12 («третья рука») по двум общим словам → `Т-10.known_by = {Заварзин: 34, Куратор: 34, Штерн: 36}` при канонном «Никто. Ответ — том 2». Наследует линтер. | `realcanon.py:492` `_match_matrix_fact` (порог ≥ 2) | да |
| 1.5 | В | Имя без «гл. N» в «Персонажи знают» = 0 («всегда»), даже когда матрица даёт главу: Т-08 «Степан» → 0 при М-14 «гл. 43–44»; Т-02 «Лемм» → 0 при матрице «гл. 3». `_merge_known` берёт `min`. Следствие: маркеры Т-08 не фильтруются из досье в главах Степана 2–42. | `realcanon.py:523, 541` | да |
| 1.6 | В | Мередит не входит в «известные имена» (источник — субъекты матрицы + таблица 03): гл. 41 `participants: []`, досье Мередита ни в одном окне; его «Опознавательный код» (перстень на левой руке, перчатка с одной руки) не разбирается. У 7 карточек из 10 нет секции «Физика». В известные имена попал «Британец» из фразы «Британец — никогда не фокален»; «Куратор» и «Куратор ОГПУ» дублируются. | `exporter.py:326`, `realcanon.py:150, 617` | да |
| 1.7 | В | Континуити 3.3 в окно не входит (только в Э2): окно гл. 6 (кабинет Лемма сразу после гл. 5) не содержит канонизированных Р-018 деталей (лампа под зелёным абажуром, венский стул, дело в левой тумбе, запах) — дрейф гарантирован. | `compile_window` | да |
| 1.8 | В | `bans` и `not_knows` брифа для реального канона всегда пусты (ср. эталонный `Тест_Писателя/ПРОМПТ_Глава5.md` §4–5: «оружие не упоминать; никаких догадок, зачем Лемм пришёл ночью»). Секция «НЕ знает» = «ещё 16 фактов недоступны». Поглавник таких полей не имеет — пробел канона + парсера. | `parse_registry_briefs`, `окно.md.j2` | да |
| 1.9 | В | Калибровка Р-015 не в окне: `_style_sections` берёт из 02 только §1–4 и §6.1; §5 «Числовые ориентиры» (абзац 2–5 фраз, диалог 15–20 %, сенсорная ткань), §6.2 (брак → норма), §6.3 (эталоны голоса) не входят. Нормы печатаются машинными id («короткая_фраза_порог: мин 6.0 макс 6.0 слов»). Доктрина §1–4 — 41 % окна. | `compiler._style_sections`, `окно.md.j2` | да |
| 1.10 | В | Сцена по Мауэри из поглавника (место · участники · цель · входит/выходит · кладём) склеивается в одну строку; «кладём:» уезжает в «Биты», где подсказка «читатель знает, что Лемму есть что защищать» (гл. 5) выдаёт Писателю тайну. | `realcanon.enrich_from_poglavnik` | да |
| 1.11 | М | Ложные участники: гл. 46 → Степан («последний рапорт Степана»), гл. 7 → Лемм; гл. 3, 33 — пусто. Закладка «Гл. 29 или 40» → обе главы получают приказ положить запонку. Континуити при двух полях теряет статус («Часы Штерна … · статус: в руках Штерна» → `note: ""`). | `realcanon.py:286, 391, 446` | да |
| 1.12 | М | Из 02 §5 теряются нормы (TTR 0,40 = брак, доля диалога, длина абзаца, сенсорная ткань); 04 раздел Е «любые кальки телепроцедурала (…)» отброшен фильтром `startswith("любые")`; словарь усилителей — 3 слова («пугающе» в принятой гл. 5 не считается). Хронология 12 и хроника 17 не экспортируются. | `realcanon.py:87, 138, 192` | да |
| 1.13 | М | Дата без года и «та же ночь» (гл. 6/40/41) не разрешаются в дату; список 0.4 велит «не употреблять — крыша; заказать; наезд; опер», хотя буквальные значения 1926 года легитимны; плейсхолдер «постройте круги и внесите в 2.1» печатается Писателю. | `окно.md.j2`, `_line_rules` | да |

## 2. Проверки Э1 и Э2

| № | Ур. | Находка | Где | Воспр. |
|---|---|---|---|---|
| 2.1 | В | Принятая гл. 5 (Р-018) не проходит Э1: `V1.2a` BRAK 6,2; `V1.2b` FLAG 62,8 %; `V1.4` FLAG 7,98/1000; 8 однострочных абзацев. Регрессия на «золотом» тексте будет красной. | `verifier1.analyze` на `Проза/Том1_Глава05.md` | да |
| 2.2 | В | TTR-окно никогда не считается: корпус по части, а часть ≤ 10 × 800 = 8 000 < 10 000 слов. Единственная защита от лексической бедности мертва для тома 1. | `verifier1.py:137, 395` | да |
| 2.3 | В | Стоп-лексика 0.4 флагует буквальные значения («Опера шла в Большом», «Крыша дома текла», «заказать обед»); пропускает «врага народа», «вредителя», «глухаря»; 0.3-Штерн не ловит «отцовский», «отечески», а «отец» в реплике Лемма в главе Штерна даёт ложный FLAG (речь не отделяется от повествователя). | `verifier1.py:60–103` | да |
| 2.4 | В | Э2 не получает того, что требуют чек-листы 4.1–4.3: досье (физика, речевые паспорта), прозаические запреты линий 03 (`build_prompt` берёт только `kind == "лексика"`), реестр документов, колонку «Что нового знает читатель», хронику 17 и хронологию 12; шаблон не содержит 4.2 (анахронизм-аудит), 4.3.3/4.3.7 («двойное дно», «глазами решившего слой 1»); правила вкуса §6.1 не проверяются вообще — а это 14 из 14 правок автора по гл. 4. Матрица печатает «(узнаёт в гл. 0)» вместо «всегда». | `verifier2.py:30–104`, `верификатор2_система.md` | да |
| 2.5 | В | Э2 п. 5 «ЛЮБОЙ факт, отсутствующий в брифе — самоволка» превращает каждый придуманный предмет (бриф прямо разрешает «предметы придумай») в самоволку с решением автора по одной кнопкой. Нет определения серьёзностей, требования дословной короткой цитаты (якорь `review._anchor` — точная подстрока), лимита числа флагов. | `верификатор2_система.md` | по коду |
| 2.6 | М | Сплиттер режет «т. е.», «и т. д.» (с пробелом), «Лемм А. Х. подписал.»; ни Э1, ни Э2 не проверяют блок `→ ДОКУМЕНТ … ← КОНЕЦ ДОКУМЕНТА`, долю диалога, однострочные абзацы. | `textutils.py`, `сокращения.txt` | да |
| 2.7 | М | `правки.md.j2`: полная перегенерация текста ради точечных замен — 33 % стоимости главы и источник самоволий. Пары БЫЛО/СТАЛО с уникальной цитатой — детерминированная операция. | `writer.apply_edits` | по коду |

## 3. Линтер канона: что не ловит и где врёт

Базовый прогон: 1 находка. Позитивные контроли (ФОКАЛ-2, ЭПИСТ-1, ХРОН-2) сработали. Ниже — мутации, на
которых линтер **молчит** (по коду `lint.py`).

| № | Ур. | Класс / пример | Воспр. |
|---|---|---|---|
| 3.1 | К | Таблицы §5 (дозы) и §6 (документы) не сверяются ни с чем: доза №1 → «гл. 13» (глава Штерна; сетка и §7 — 12); документ №2 → «после гл. 9» (сетка/поглавник — 8). | да |
| 3.2 | В | Дата главы vs период части (гл. 10 «27.04» при части II «май–июнь»); событие брифа vs ✓-дата хроники 17 («Смерть Дзержинского» 15.07 при ✓ 20.07); дни недели; **ложная ХРОН-2 на стыке года** («30 декабря» → «2 января»). | да |
| 3.3 | В | Эпистемика матрицы: субъект «узнаёт» в главе, где его нет (М-04 Лемм «гл. 16» — глава Штерна); читатель узнаёт через фокал раньше фокала; знание раньше события (Штерн о фальсификации «гл. 30», событие гл. 32); автор действия узнаёт позже читателя. | да |
| 3.4 | В | Раскрытие тайны читателю в главе, фокал которой её не знает (Т-09 → гл. 43, Степан). | да |
| 3.5 | В | Закладки: зола Лемма → «Гл. 7» (глава Штерна); «Печь в мае» → гл. 45 при §5 «только доза №1 и гл. 46»; ЗАКЛ-2 мёртв для реального канона (выстрелы в §7 без глав). | да |
| 3.6 | В | Досье vs остальное: «глухота на правое ухо» при 33 «левое»; статус Заварзина «гибнет т.4» при Ф-1931-04; возраст в абсолютном году («19 лет в 1913-м» при рожд. 1896; «45 (1926, кабаре)»). | да |
| 3.7 | В | Поглавник vs реестр: «## Гл. 3 · 17 апреля · фокал ШТЕРН» при сетке «3 · Лемм · 15.04»; акты «19–35 / 36–46» при колонке «Части III–IV / V»; гл. 46 → Лемм против пропорции фокалов §1.6. | да |
| 3.8 | В | «Персонажи знают» vs матрица без явных глав не сравнивается: в реальном каноне Т-04 «Никто из героев» vs М-05 Штерн «всегда»; Т-03 без Заварзина; Т-06 без Штерна; Т-08 «Степан» vs гл. 43–44. | да |
| 3.9 | В | Ложные ЭПИСТ-1/ПРОЗА-1: маркер = подстрока, реплика чужого персонажа = знание фокала: бриф гл. 26 «молодец, сынок» → «сын» Т-05, «активно продолжается» → «актив» Т-01; гл. 33 «после приговора» → Т-06. | да |
| 3.10 | М | ДОСЬЕ-2 пропускает [[Александр-сын]]; персонажи сцен без досье (Клюев, куратор, Холодова, поляк, посредник) не отмечаются; `check_prose` не прогоняет нормы 02 §5 по принятой прозе и не сверяет прозу с континуити («старым табаком и крепким чаем» vs 33 «слабый табак»). | да |
| 3.11 | М | Производительность и гигиена: `_find_line` перечитывает файл на каждую находку; `check_secrets_vs_matrix` делит реестр на каждую тайну; `_library_docs`/`_registry` считаются дважды; `canon-commit` делает экспорт дважды; имена в `parse_focal_volumes` без `re.escape`. | по коду |

## 4. Бэкенд: надёжность, гонки, масштаб, безопасность

| № | Ур. | Находка | Где | Воспр. |
|---|---|---|---|---|
| 4.1 | К | Повторный `canonize --apply` дублирует записи: сбой между `apply_batch` (коммит сделан) и `st.transition("зафиксировано")` → библиотека чистая, FSM «принято», повтор применяет пакет ещё раз (строка самоволки ×2, второй коммит `[глава N]`). Проверки идемпотентности перед записью нет. | `cli.py:432–434`, `canonist.py:245–260` | да |
| 4.2 | К | `rollback N --to <опечатка>` для зафиксированной главы сначала делает `git revert`, потом валидирует `to`: канон откачен, FSM «принято», пользователю — «Неизвестное состояние». | `cli.py:916–949` | да |
| 4.3 | К | `gitops.revert` при конфликте бросает до `--abort`: в документе канона маркеры `<<<<<<<`, индекс unmerged, `REVERT_HEAD`; `restore_library` падает, `dirty()` блокирует `--apply`, `canon-commit` закоммитит маркеры. | `gitops.py:73–86`, `cli.py:934–938` | да |
| 4.4 | В | Сбой линтера не-MarkupError (файл в cp1251 → `UnicodeDecodeError`) в потоке наблюдателя глотается, `lint_pending` остаётся True, каждый GET `/api/lint` заново делает экспорт + линт и отвечает 500; `run_lint_now` ловит `RuntimeError` как «занято», хотя его бросают линтер, экспортёр и `typer.Exit`. | `server.py:475–507`, `canonwatch.py:57–60` | да |
| 4.5 | В | GET `/api/lint` пишет на диск (полный экспорт), экспорт из потока наблюдателя подменяет `exports/` под ногами у CLI-команды в терминале; `export_corpus` переписывает все файлы корпуса с `fsync` при каждом экспорте (при 506 главах — секунды внутри HTTP-ответа на сохранение); на Windows `os.replace` падает `PermissionError`, если другой поток читает файл. | `server.py:495–497`, `exporter.py:447–463`, `guard.py:70–85` | да / по коду |
| 4.6 | В | `lint-llm`: `files` не ограничены библиотекой (`../секрет.txt` уходит в Anthropic и в `logs/линтер_промпты/`); прерывается на первом документе без JSON — находки проверенных документов теряются; нет лимита стоимости. | `cli.py:812`, `server.py:597`, `lint.py:544–562` | да |
| 4.7 | В | `итераций_правок` никогда не сбрасывается: после трёх итераций любой будущий цикл правок отказывает даже после отката в «собрано». | `fsm.py:142–145`, `cli.py:276` | да |
| 4.8 | В | Один битый файл кладёт панель: пустой `status.yaml` любой главы → `/api/state` 500 для всех; то же для `regression/report.json` и устаревшей схемы `briefs.json`. `/api/state` на каждый опрос парсит YAML всех глав и хэширует шаблоны (0,6 с при 506 главах). | `fsm.py:61–67`, `server.py:203–246` | да |
| 4.9 | В | Правки из вида «Канон» и `apply_fix` пишут в библиотеку без коммита и журнала; `_canon_path` позволяет создать любой новый `.md`, включая `Проза/ТомN_ГлаваNN.md` — текст попадает в корпус мимо FSM и приёмки. | `server.py:514–547` | да |
| 4.10 | В | `cmd_lint` завершается `typer.Exit(1)` при ошибках канона → задача `lint` в панели помечается «ошибка», хотя отчёт построен; «Проверить моделью» молча пропускает LLM-слой при машинных ошибках. | `cli.py:811–838` | да |
| 4.11 | М | Отрицательный `Content-Length` блокирует поток хендлера; `_retryable` ретраит `TypeError` 3 раза; `write_atomic` меняет права на 0600; коммит `revert` без `--author` (Д-8); наблюдатель — только mtime без размера, `_snapshot` без замка; `rollback` — `KeyError` на статусе без «истории»; `JobRunner.ensure_idle` мёртв; `prompt("edits")` отдаёт промпт прошлой итерации; CRLF→LF при сохранении из панели даёт диф на весь файл. | `server.py`, `adapters.py:39–49`, `guard.py:74`, `gitops.py:85`, `canonwatch.py` | да / по коду |
| 4.12 | М | AST-тест «нет записи мимо guard» не видит `os.replace/rename`, `unlink`, `rmtree`, `subprocess` и не проверяет, кто открывает `canon_write_session()` (открывателей уже четыре). | `tests/test_stage5_static.py:40–72` | по коду |

## 5. Панель и UX

Безопасность: XSS не найдено (единственный `dangerouslySetInnerHTML` получает экранированную
подсветку, `flag_id` ограничен паттерном), CDN нет, бандл 180 КБ (59 КБ gzip). Производительность на
46 главах и 32 документах: `/api/state` 3 мс. Узкое место — не сервер, а потоки автора.

| № | Ур. | Находка | Где | Воспр. |
|---|---|---|---|---|
| 5.1 | К | Несохранённые правки канона теряются молча при уходе из вида «Канон» (клик по главе, «Дашборд», Enter в поиске); диалог есть только при смене документа внутри вида. | `App.tsx:171–202`, `Canon.tsx` | да |
| 5.2 | К | Конфликт версии — тупик: 400 «перечитайте», но нет кнопки «перечитать/сравнить»; «Отменить правки» возвращает устаревший текст. Сервер не различает конфликт от прочих ошибок. | `Canon.tsx:83–91`, `server.py` | да |
| 5.3 | В | Текст во вкладках главы теряется при переключении: edits.md, пакет канониста, вставленный черновик (20–30 КБ) живут в state размонтируемых компонентов; индикатора «не сохранено» нет. | `ChapterView.tsx:160–164, 302–307, 484–487` | да |
| 5.4 | В | Обрыв связи = «TypeError: Failed to fetch» по-английски, кнопки активны, тосты каждые 2,5 с. Неизвестный GET-путь API отдаёт `index.html` с 200 → `r.json()` падает → `{}` как успех. Ошибки сервера — сырые исключения с абсолютными путями. | `App.tsx:45–60`, `api.ts`, `server.py:695–708` | да |
| 5.5 | В | Длинные задачи без обратной связи: Писатель 1–3 мин без таймера; круги — 51 вызов без «N из M»; нет отмены; результат в ручном режиме свёрнут в «показать вывод»; глобальные задачи («lint готово») висят в карточке любой главы, задача другой главы не видна. | `ChapterView.tsx:117–186`, `cli.py:163–170` | да |
| 5.6 | В | Подсказка «Дальше:» — команды CLI («ugar apply-edits 1») в панели и в review.html. Правки вводятся руками в формате БЫЛО/СТАЛО с дословной цитатой — нет «выделил фрагмент → заменить». Самоволки решаются по одной, без «вычеркнуть все». | `cli.py NEXT_STEP`, `ChapterView.tsx` | да |
| 5.7 | В | «Машинное время» включает простой автора (ожидание клика в `собрано`/`сгенерировано`/`верифицировано-1`): метрика критерия приёмки «≤ 40 мин автора» искажена. | `timing.py:13, 34` | да |
| 5.8 | В | Модальное подтверждение без ловушки фокуса (Tab уходит в сайдбар); гонка `runCommand` ↔ опрос (`/api/state`, стартовавший до POST, перезаписывает `job` → секунда с активными кнопками). | `Confirm.tsx`, `App.tsx:45–89` | да / по коду |
| 5.9 | М | Доступность: `div role=listitem` с `onClick` вместо кнопок, вкладки без стрелок/`tabpanel`, `<a href="#">`, `<mark>` без клавиатуры; поиск обрезает по 20 без пометки; журнал API — 30 строк без ошибок; дашборд в тёмной теме с прозрачным фоном; review.html показывает дифф всей главы при перегенерации; `commit()` очищает сообщение до результата; компоновка «Канон» на 1024 px; `/api/chapter/999` отдаёт синтетическую «не начата». | `App.tsx`, `Canon.tsx`, `dashboard.py:31`, `htmlreview.py:176` | да |

## 6. Продукт и процесс

**Драматургия.** Круг Хармона применён на всех трёх уровнях одинаково: 8 шагов по 2–4 фразы для
каждой из 46 глав объёмом 700–800 слов (одна сцена). Это 46 мини-арок с обязательным «Изменением» в
финале — конфликт с ремеслом сцены Мауэри (цель → конфликт → исход; сиквел: реакция → дилемма →
решение) и с 05 §3. Материал для круга тома — только однострочные события + реестр тайн: ни темы цикла
(13 §1), ни арок персонажей. «Внести в канон» вносит 51 круг (~15 000 слов) одним подтверждением —
формально FR-K3 соблюдён, по духу «все канонические решения за автором» — нет. Арки Уэйланда после
Р-022 из окна ушли и ничем не заменены (п. 28 прошлого плана).

**Цикл автора на главу:** 10–15 кликов, 3 подтверждения, два свободных текста (правки, пакет), четыре
последовательных ожидания моделей. Узкие места: дословные цитаты в правках; самоволки по одной; пакет
Канониста как сырой markdown; брак Э1 по средней фразе даёт 3 генерации (причина — окно, п. 1.9); нет
второго прохода Э2 после правок; `backup --push` только из CLI.

**11 томов:** `chapters/005` не знает тома; `circles.CANON_DOC = "21_Круги_истории_Том1.md"`
захардкожен; нет «закрыть том» (снапшот 3.5, тег, статистика, сборка docx/epub); версии канона без
тегов; A/B вариантов главы нет; `chapters/`, `logs/`, `круги_истории/`, `snapshots/` в `.gitignore` —
черновики, правки автора, флаги и журнал API не бэкапятся; библиотека внутри репозитория кода.

**Экономика** (`ЭКОНОМИКА.md` сверена с кодом): окно ≈ 4 000–4 600 токенов, Э2 ≈ 4 900, круги 51
вызов — верно. Не учтены `lint --llm` (вызов на документ, ≈ $0.5–1 за прогон, запускается кнопкой) и
`regress --llm`. `writer.params = {}` — thinking Gemini не задан, смета ±40 %. Кэширование промптов не
поможет (первая строка окна уже переменная; system Э2 ниже минимума). Экономия без потерь: БЫЛО/СТАЛО
кодом (−33 % главы), Batch API для кругов/линта/регрессии. Тратить больше оправдано: Э2 на старшей
модели (+$0.03/глава), второй Э2 после правок (+$0.04), A/B два черновика (+$0.17), самопроверка
Писателя по §5/§6.1 (+$0.15 вместо трёх генераций).

**Риски:** пины моделей не сверяются с API (снятие модели = 404 на горизонте лет); SDK без верхних
границ и lock-файла; prompt-injection: текст главы в промпте Э2 идёт после инструкций без ограждения,
пакет Канониста читается как markdown в textarea; один remote, рабочая область без бэкапа; массовая
канонизация кругов и широкий п. 5 Э2 замусорят канон и утомят автора.

## 7. Качество самого канона (решения автора)

| № | Ур. | Находка | Где |
|---|---|---|---|
| 7.1 | В | Фокал Аси: реестр §1 «Ася и Заварзин… линии открываются в томах 2–3» vs 03 (Ася — 4–6) vs досье «Фокал: с т.4». | реестр:12, `03_…md:17–19`, досье Аси:48 |
| 7.2 | В | Реестр тайн vs матрица «кто знает»: Т-04 «Никто из героев» при М-05 Штерн «всегда»; Т-03 без Заварзина (М-04 «всегда»); Т-06 без Штерна (М-07 «всегда»); Т-08 «Степан» без главы vs М-14 «гл. 43–44»; Т-07 «гл. 17–19» vs М-09 «гл. 18». | реестр §2 vs `31_…md` |
| 7.3 | В | Объём тома: реестр §3 «46 глав, ~520–560 стр.» vs Р-019 «700–800 слов/глава» → ≈ 35 тыс. слов ≈ 90–100 стр. | реестр:61, Р-019 |
| 7.4 | В | Р-015 vs Р-018: тем же днём канон объявляет «средняя < 7 — брак», и гл. 5 «принята без правок вкуса» при средней 6,2 (см. 2.1). | `36_…md`, `Проза/Том1_Глава05.md` |
| 7.5 | В | Хронология «Континенталя»: сеть перехватывает запросы в 1916 (Ф-1916-02), знак консорциума на документе вербовки в октябре 1917 (Ф-1917-02), а консорциум создают капиталы, «национализированные в 1917-м» (декабрь) — сеть старше причины своего возникновения. | `12_…md:23–29`, `14_…md` |
| 7.6 | В | Персонажи сцен без досье: куратор ОГПУ, Клюев, Холодова (гл. 37–41), поляк-резидент, посредник, оперативник гл. 17; у Заварзина, Аси, Бугаева нет «Физики» — Писатель обязан выдумать внешность. | `Досье/*` |
| 7.7 | М | Мередит «Т.3: рука британской линии в третьей руке тома 1» vs §7 «Третья рука — Том 2»; Ф-1926-10 «октябрь [гл. 31–32]» при гл. 31 = 28.09; Ф-1926-02 «15.04 сжигает» при сожжении в гл. 6 (ночь 18.04). | досье Мередита:68, `12_…md:50, 58` |
| 7.8 | М | П6 после приёмки: отчество «Ильич» (гл. 4) ни в досье, ни в 33; «Обещание куратора» в 33 без строки в 3.1; 33 «слабый табак» vs проза «крепким чаем». Пробелы: 1.6 Топография ⬜ при пяти «адрес ⚠» в поглавнике; 2.4, 3.4 ⬜; «формула вежливости — сформулировать» (гл. 41); индекс 00 «Р-001…Р-014» при Р-022. | `Проза/…Глава04`, `33_…md`, `00_ИНДЕКС` |

## 8. Качество как ПО

- **Тесты:** 228, покрытие 91 %; хорошие гарантии (AST «нет порогов», «нет записи мимо guard», сканер
  окон). Пробелы: нет теста «окно содержит §5/§6.3» (потому что не содержит), на обрывки списков после
  фильтра, на многотомность, на промпт Э2 с прозаическими запретами, на идемпотентность
  `canonize --apply`, на `rollback --to` с опечаткой, на conflict в `revert`.
- **CI:** Ubuntu + Windows, 3.11/3.12, job с SDK, проверка сборки панели. Нет macOS
  (`Запустить_панель.command` никем не проверяется), нет `ruff`.
- **Упаковка:** `version = "1.0.0"` статична, тегов нет, CHANGELOG нет, `ugar --version` нет; SDK без
  верхних границ и lock-файла (`anthropic 1.x` сменил HTTP-стек). Для автора «обновиться» = `git pull`
  в его же репозитории с его каноном.
- **Документация:** README/ЗАПУСК согласованы, но писали «215 тестов» (исправлено в этом коммите);
  ЭКОНОМИКА не учитывает lint/regress. `cli.py` — 1266 строк; сервер вызывает typer-функции и
  разбирает `typer.Exit` — хрупко.
- **Сохранность и §1.3 ТЗ:** рукопись и канон лежат на GitHub при запрете «никакого облака» — либо
  сознательно принять приватный репозиторий как бэкап, либо вынести библиотеку в локальный remote.

---

## План усовершенствования

Порядок — по риску для канона и рукописи, затем по эффекту на качество текста. Каждый пункт
заканчивается тестом, который ловит именно эту ошибку. «Р-№» — нужно решение автора в журнале 3.6.

### Этап 1. Срочные исправления (1–2 дня, Р-№ не нужны)

1. **Идемпотентная приёмка (4.1):** SHA/хэш пакета в `status.yaml` до применения; отказ при найденном
   `[глава N]`; `transition` и коммит — в одной защищённой последовательности.
2. **Git в безопасной обёртке (4.2, 4.3):** валидация `--to` до побочных эффектов; `revert` в
   `try/finally` с `--abort`; проверка `REVERT_HEAD/MERGE_HEAD` в `doctor` и перед каждой записью в
   канон; `--author` всюду.
3. **Линтер как фоновая очередь (4.4, 4.5, 4.10):** наблюдатель и панель только кладут запрос; один
   рабочий поток через `JobRunner`; любое исключение — находка `ЛИНТ-0` в отчёте; GET `/api/lint`
   ничего не пишет; `cmd_lint` возвращает код, а не бросает `Exit` в панели; «Проверить моделью»
   объясняет пропуск.
4. **`lint --llm` (4.6):** `files` только внутри библиотеки; per-document `try/except` с частичным
   результатом; лимит вызовов/стоимости и предупреждение о цене перед запуском.
5. **FSM (4.7, 4.8):** `rollback` сбрасывает `итераций_правок`/`авто_повторов`; битый `status.yaml`
   → состояние «повреждено» главы, не 500; `status.yaml` через pydantic с версией схемы.
6. **Панель — защита текста автора (5.1–5.3):** единый «грязный» флаг на уровне `App` + диалог при
   смене вида/вкладки + `beforeunload`; черновики в `localStorage`; при конфликте версии — «Показать
   различия / Перечитать (правки в буфер) / Перезаписать»; отдельный код ошибки 409.
7. **Разбор канона — течи (1.3, 1.4, 1.5):** список, из которого отфильтрован хотя бы один элемент,
   убирается целиком; скобочные инструкции инструменту вырезаются; `_FUTURE_RE` — по всем ссылкам
   фразы, не по первой; порог `_match_matrix_fact` ≥ 3 слов или явная ссылка на факт; имя без главы в
   реестре при главе в матрице → глава матрицы. Тест: скан 46 окон на «расчёт», «к браку», «;» в
   конце строки отношений.
8. **Гигиена:** `ruff --fix` (13 импортов), `zip(strict=)`, README/ЗАПУСК «228», ложная ХРОН-2 на
   стыке года, `canon-commit` без двойного экспорта.

### Этап 2. Что видит Писатель (2–3 дня; Р-№ на состав секций)

9. **Экспорт §5 доз и §6 документов реестра и колонки «Что нового знает читатель» (1.1, 1.2):** в
   бриф — `doses`, `documents` с триггером/содержанием/запретами/стилем; в окно — отдельные секции
   «Документ-вставка» и «Доза прошлого» с правилом доз. Тест: окна 12/14/21/22/32/35/44/46.
10. **Секция «Что было раньше (глазами фокала)» (1.7, 1.8):** события глав ≤ N из сетки 2.2, где
    фокал — участник; срез 3.3 по месту/предметам сцены; последние 2–3 абзаца предыдущей главы той же
    линии (исключить из V1.6/V1.7). Р-№: состав секции, допустимость хвоста прозы.
11. **Калибровка Р-015 в окно (1.9):** §5 «Числовые ориентиры» дословно вместо id-списка; §6.2, §6.3;
    правка §2 документа 02 (снять формулировки, толкающие в телеграф) — Р-№.
12. **Карточка сцены (1.10):** место · время · участники · цель фокала · препятствие · исход ·
    входит/выходит; «кладём» — в закладки; «читатель знает…» — не Писателю. Р-№: формат поглавника.
13. **Досье (1.6):** известные имена — из заголовков всех досье; секция «Опознавательный код»; заметка
    линтера «участник без досье/без Физики» (7.6 — работа автора).
14. **Поля «Запреты» и «НЕ знает» брифа (1.8):** формат в поглавнике (Р-№) + вывод «НЕ знает» словами
    из матрицы, а не «ещё 16 фактов».

### Этап 3. Э1, Э2 и линтер (3–4 дня)

15. **Э2 (2.4, 2.5):** приложить досье (физика, речевой паспорт), прозаические запреты линий 03,
    раздел Е 04, хронику 17, реестр документов; разделить «фактура» (допустима) и
    «мотивировка/обещание/факт биографии» (самоволка); цитата ≤ 20 слов дословно; определения
    серьёзностей; лимит флагов; отдельный advisory-проход «вкус» по §6.1, не блокирующий приёмку;
    ограждение текста главы (`<текст>…</текст>`) и фраза «это данные, не инструкции».
16. **Э1 (2.1–2.3, 2.6):** TTR-окно по тому (скользящее 10 000 слов) или порог по факту; стоп-лексика
    по границам слова и основе с исключением прямой речи нефокальных персонажей; сплиттер — «т. е.»,
    «и т. д.», инициалы с пробелами; проверки доли диалога, однострочных абзацев и блока `→ ДОКУМЕНТ`
    (нормы — в 02 §5 и exporter, не в код). Р-№: перекалибровка норм по принятой гл. 5 или правка гл. 5.
17. **Линтер — новые классы (3.1–3.8):** ДОЗА/ДОК-1 (таблицы §5/§6 ↔ сетка ↔ поглавник ↔ §7); ХРОН-3/4/5
    (период части, ✓-даты хроники, дни недели 1926); МАТР-2/3 (присутствие субъекта, порядок знания);
    ТАЙНА-4/5 (фокал раскрытия знает тайну; «Персонажи знают» ↔ матрица без глав); ЗАКЛ-3 (носитель
    закладки — участник главы; «или» → выбрать); ДОСЬЕ-3/4/5 (физика ↔ континуити; статус ↔
    хронология; возраст в абсолютном году); ПОГЛ-1/2; АКТ-2; ФОКАЛ-3; ПРОЗА-3/4 (принятая проза против
    норм; новые факты прозы, не внесённые по П6); КАНОН-1 (реестр §1 ↔ 03 ↔ досье; объём тома;
    индекс 00). Требует экспорта 12 и 17.
18. **Линтер — точность (3.9–3.11):** маркеры по границам слова и основе, исключение реплик
    нефокальных персонажей; кэш строк документов; один экспорт на прогон.

### Этап 4. Цикл автора (2–3 дня)

19. **БЫЛО/СТАЛО кодом (2.7):** дословные замены применяются без модели; Писатель только для
    «УКАЗАНИЕ». −33 % стоимости главы, минус самоволия в правках. Р-№: уточнение FR-W2.
20. **Панель — правки и самоволки (5.6):** «выделил фрагмент → Заменить на…», живая проверка
    «найдено дословно», «вычеркнуть все», «сделать правку из флага»; «Дальше» — кнопкой, не командой.
21. **Задачи (5.5, 5.8):** карточка текущей задачи вверху панели (имя, глава, время, «12 из 51»),
    кнопка «Остановить» (флаг отмены между вызовами), развёрнутый результат ручного режима с
    переходом на вкладку; `prevJob` по `started`; ловушка фокуса в модальном окне.
22. **Связь и ошибки (5.4):** баннер «Сервер недоступен» с блокировкой кнопок; русские ошибки с кодами
    (404/409/423/500) без абсолютных путей; неизвестный `/api/*` → 404 JSON.
23. **Время такта (5.7):** авторское = любое ожидание клика; «сегодня: N мин автора».
24. **Второй Э2 после правок** одной кнопкой (advisory); A/B `write --варианты 2` с метриками Э1 рядом.

### Этап 5. Архитектура и 11 томов (4–6 дней)

25. **Один конвейер изменения канона (4.9):** `canon_change(...)` = сессия записи → экспорт → линт →
    коммит или явное состояние «незакоммичено» в панели; правки из вида «Канон» не могут создавать
    файлы в `Проза/`.
26. **Инкрементальный экспорт и кэш состояния (4.5, 4.8):** писать только изменившиеся выгрузки;
    корпус — только для изменённых глав; сводки глав в памяти с инвалидацией по mtime.
27. **Многотомность:** ключ `(том, глава)` во всех артефактах, `chapters/Т1/001`, документы 2.1/2.2/3.1
    по тому, `ugar volume close` (снапшот 3.5, тег `том-N`, статистика, сборка docx локально).
28. **Библиотека — отдельный git-репозиторий** (sibling, `library_dir`), второй remote; тег
    `глава-N` при приёмке. Р-№: где хранить (GitHub vs §1.3 ТЗ).
29. **Бэкап рабочей области:** архив `chapters/ logs/ круги_истории/ snapshots/` в заданную папку после
    каждого `canonize --apply`; кнопка в панели.
30. **Разделить `cli.py`:** логика шагов в `ugar/steps/*.py` с обычными исключениями; typer и сервер —
    тонкие обёртки; единый обработчик ошибок. AST-тест расширить на `os.replace/unlink/rmtree` и на
    список открывателей `canon_write_session()`.
31. **Пины и SDK:** `doctor` сверяет ID моделей с API; lock-файл; `thinking` Gemini в `params`;
    `ugar --version`, теги релизов, CHANGELOG; macOS и `ruff` в CI.

### Этап 6. Драматургия (Р-№, работа автора + 2–3 дня)

32. **Круги по уровням:** том и акты — 8 шагов; глава — «шаг акта, на который работает» + сцена/сиквел
    по Мауэри; круги глав — черновик, не канон; внесение в канон по уровням с диффом и правкой текста
    перед внесением. Р-№: пересмотр Р-020 в части глав.
33. **Документ «Арки тома»** (Уэйланд: персонаж × акт — ложь/желание/потребность/где на арке) с
    фильтром по знанию фокала — в материал кругов и одной строкой в окно. Р-№; задача автора.
34. **Материал кругов:** тема цикла (13 §1), арки, события с участниками — вместо однострочников.

### Оценка объёма

| Этап | Пунктов | Ориентир | Решения автора |
|---|---|---|---|
| 1. Срочные исправления | 8 | 1–2 дня | нет |
| 2. Что видит Писатель | 6 | 2–3 дня | состав секций, формат поглавника, §2 документа 02 |
| 3. Э1, Э2, линтер | 4 группы | 3–4 дня | перекалибровка норм по гл. 5 |
| 4. Цикл автора | 6 | 2–3 дня | FR-W2 |
| 5. Архитектура и 11 томов | 7 | 4–6 дней | место хранения библиотеки |
| 6. Драматургия | 3 | 2–3 дня + автор | Р-020 (главы), «Арки тома» |

**Быстрые победы за один день** (без решений автора): пп. 1–5, 7, 8, 11 (включение §5/§6 без правки
§2), 15 (разделение «фактура/самоволка», цитаты, ограждение текста). Самое дорогое по эффекту на текст —
пп. 9, 10, 12, 32, 33, и именно они требуют записей Р-№.

### Решения, которые нужны от автора (Р-№)

1. Состав секции «Что было раньше» и допустимость хвоста прозы предыдущей главы в окне.
2. Правка 02 §2 под Р-015; перекалибровка норм по принятой гл. 5 или правка гл. 5 (7.4).
3. Формат карточки сцены и полей «Запреты»/«НЕ знает» в поглавнике.
4. Круг Хармона для глав: шаг акта + сцена/сиквел вместо полного круга (пересмотр Р-020).
5. Документ «Арки тома» — писать ли, в каком формате.
6. Где хранить библиотеку: отдельный репозиторий, второй remote, GitHub vs «никакого облака».
7. Противоречия канона 7.1–7.8 (фокал Аси, реестр тайн vs матрица, объём тома, хронология
   «Континенталя», досье второго круга, П6 после гл. 4–5).
8. Пины моделей для Э2/Канониста/кругов и `thinking` Писателя (Д-11, через `retest`).

---

## Статус выполнения

| Этап | Состояние | Что сделано |
|---|---|---|
| 1. Срочные исправления | ✅ выполнен (08.09.2026) | идемпотентный `canonize --apply` (восстановление по действующему коммиту, откачённая приёмка не считается); `rollback --to` проверяется до побочных эффектов; `revert` с `--abort` при любом сбое и подписью автора; `in_progress` блокирует запись в канон и виден в `doctor`; сброс бюджетов FSM при откате; битые `status.yaml`/отчёт регрессии не роняют панель; линтер — одна фоновая очередь, GET без побочных эффектов, сбой = ЛИНТ-0; `lint --llm` только внутри библиотеки, частичный результат, `--лимит`, `--no-strict` для панели; панель: единый реестр «не сохранено» + диалог при уходе + `beforeunload`, черновики в `localStorage`, конфликт 409 с «различия / перечитать / перезаписать» (проверено в Chromium); фильтр досье: списки с вырезанным элементом убираются целиком, все ссылки на тома во фразе, инструкции инструменту вырезаются, маркеры Т-07 уточнены (Р-022, уточнение), `_match_matrix_fact` ≥ 3 основ или явная ссылка, имя без главы берёт главу матрицы (Т-02, Т-07, Т-08, Т-09; Т-10 больше не матчится с М-12); гигиена: импорты, `zip(strict=)`, ХРОН-2 на стыке года, один экспорт в `canon-commit`, `ruff` в CI. Тесты: 247 |
| 2. Что видит Писатель | ⏳ (п. 13 выполнен, 08.09.2026) | п. 13 «Досье»: известные имена — субъекты 3.1 + линии 03 + имена всех карточек досье (Мередит, Ковров, Ремез); «Британец» из оговорки таблицы 03 и дубль «Куратор» исключены; секция «Опознавательный код» → `Dossier.code`, в окне через фильтр Р-022; участник события — только участвующий в действии (гл. 6, 7, 8, 17, 18, 44, 46 без ложных участников), Мередит в гл. 41 из арки досье; линтер `ПОГЛ-2` (участник без досье) и `ДОСЬЕ-6` (нет «Физики») — заметки. Остальное: нужны решения автора — состав секции «Что было раньше», формат карточки сцены и полей «Запреты»/«НЕ знает», правка 02 §2 |
| 3. Э1, Э2, линтер | ⏳ | |
| 4. Цикл автора | ⏳ | |
| 5. Архитектура и 11 томов | ⏳ | |
| 6. Драматургия | ⏳ | решения Р-№ |
